
/**
 * * basic boolean value declaration and initialization
 */
let pending: boolean;
pending = true;
pending = false;

/**
 * * The following will raise an error as type number is not assignable to type boolean 
 */
// pending= 23